tables:
  users:
    columns:
      id:
        type: int
        id: true
        pk: true
      name:
        type: varchar
        length: 50
        notNull: true
      age:
        type: int
        notNull: true
      user_group_id:
        type: int
        foreignKey:
          user_groups.id:
            update: 'cascade'
            delete: ''
   #   address:
   #     type: text
      memo:
        type: varchar
        length: 300
    indexes:
      ix_name:
        columns:
          name: ASC
    #  ix_address:
    #    columns:
    #      address: desc
    # mysqlではtextなどにインデックスを張る場合length(<=255)の指定が必要
  user_groups:
    columns:
      id:
        id: true
        type: int
      name:
        type: varchar
        length: 100
        notNull: true
    indexes:
      ix:
        columns: 
          name: asc
          