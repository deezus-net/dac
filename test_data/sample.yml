tables:
  users:
    columns:
      id:
        type: int
        id: true
        pk: true
      name:
        type: varchar
        length: 100
        notNull: true
      age:
        type: int
        notNull: true
    #  user_group_id:
    #    type: int
    #    foreignKey:
    #      user_groups.id:
    ##        update: 'cascade'
     #       delete: ''
      address:
        type: text
    indexes:
      ix_name:
        columns:
          name: asc
    #  ix_address:
    #    columns:
    #      address: desc
    # mysqlではtextなどにインデックスを張る場合length(<=255)の指定が必要
  user_groups:
    columns:
      id:
        id: true
        type: int
      name:
        type: varchar
        length: 100
        notNull: true
    indexes:
      ix:
        columns: 
          name: asc
          