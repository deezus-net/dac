tables:
  users:
    columns:
      id:
        type: int
        id: true
        pk: true
      name:
        type: varchar
        length: 100
        notNull: true
  #    age:
  #      type: int
  #      notNull: true
      user_group_id:
        type: int
  #      foreignKey:
  #        user_groups.id:
  #          update: 'cascade'
  #          delete: ''
      address:
        type: text
    indexes:
      ix_name:
        columns:
          name: asc
      ix_address:
        columns:
          address: desc
  user_groups:
    columns:
      id:
        id: true
      name:
        type: varchar
        length: 100
        notNull: true
    indexes:
      ix:
        columns: 
          name: asc
          